{% extends "layout.njk" %}

{% block content %}
 <header class="blog-header">
  <h1>{{ title }}</h1>
  <p>Algunas reflexiones sobre escribir, la vida y todo lo que se nos vaya ocurriendo en el camino.</p>
  </header>
 <section>
  <div class="content blog">
    <div class="posts-grid flex">
    {% for post in collections.posts %}
    <article class="post-card">
        {% if post.data.thumbnail %}
          <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}">
        {% endif %}
        {% if post.data.date %}
          <p class="date">📅 {{ post.data.date | fechaES }}</p>
        {% endif %}
        <h4 class="post-title">
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h4>
        {% if post.data.tags %}
        <div class="post-tags">
          {% for tag in post.data.tags %}
            <span class="tag">{{ tag }}</span>
          {% endfor %}
        </div>
        {% endif %}
    </article>
    {% endfor %}
    </div>
  </div>
</section>
{% endblock %}
